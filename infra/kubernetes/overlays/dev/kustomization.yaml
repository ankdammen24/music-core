apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
nameSuffix: -dev
patches:
  - target:
      kind: ConfigMap
      name: music-core-config
    patch: |-
      - op: replace
        path: /data/WEB_API_BASE_URL
        value: http://api-dev:3000
